# ECE143_Music_Genre_Prediction
 We will use predictive modeling to find our conclusion. Then use Time Series prediction to forecast the future top genres. In parallel, we will use Correlation Analysis to find if correlations exist between genres, and use that to strengthen our prediction.
